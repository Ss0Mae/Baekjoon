def solution(numbers):
    # 1. 모든 숫자를 문자열로 변환 (문자열끼리 비교하기 위해)
    numbers = list(map(str, numbers))
    
    # 2. 정렬 기준 설정: 문자열을 3번 반복했을 때 큰 순서대로(내림차순)
    # x*3을 하는 이유: 3 vs 30 -> 333 vs 303030 -> 333이 더 큼 -> 3이 먼저 옴
    numbers.sort(key=lambda x: x * 3, reverse=True)
    
    # 3. 문자열 합치기
    answer = ''.join(numbers)
    
    # 4. [예외 처리] "000" 같은 경우 "0"으로 바꿔줘야 함
    # 숫자가 [0, 0, 0]일 때 join하면 "000"이 되는데, 정답은 "0"이어야 함
    return str(int(answer))

# 💡 파이썬 꿀팁: 문자열 3번 반복 (x * 3)
# 파이썬에서는 이 비교를 아주 쉽게 하는 꼼수가 있습니다. 문제 제한사항에 **"원소는 0 이상 1,000 이하"**라고 되어 있죠? 숫자가 최대 4자리(1000)라는 뜻입니다.
# 문자열로 변환한 뒤 3번 반복해서 비교하면 마법처럼 정렬 기준이 맞춰집니다.

# 3 vs 30
# 그냥 비교: "30" > "3" (사전 순, 길이가 긴 게 보통 뒤로 감) -> 결과: 303 (땡!)
# 3번 반복 비교:
# 3 -> "333"
# 30 -> "303030"
# 문자열 비교는 앞에서부터 한 글자씩 따집니다.
# 첫째 자리: '3' == '3'
# 둘째 자리: '3' > '0' (승리!)
# 결과: 3이 30보다 우선순위가 높음 -> 330 (정답!)